/// <reference types="cypress" />

describe('Cypress smoke test', () => {
  // it('메인 페이지가 정상적으로 열리는지 확인', () => {
  //   cy.visit('http://localhost:3000');
  //   cy.contains('히히').should('exist'); // "영화"라는 텍스트가 있으면 통과 (예시)
  // });
});
