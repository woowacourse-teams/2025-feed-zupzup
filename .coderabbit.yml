language: ko-KR

reviews:
  profile: chill
  high_level_summary: true
  sequence_diagrams: false
  auto_review:
    enabled: true
    base_branches: ["fe/develop"]

  path_instructions:
    - path: "frontend/**/*.{js,jsx,ts,tsx}"
      instructions: |
        team-conventions.md의 프론트엔드 규칙과 아래 React 코드 품질 기준을 바탕으로 리뷰하세요.

        ------------------------------------------------------------
        🔥 React 코드 품질 기준
        ------------------------------------------------------------

        1) **상태 관리 (State)**
        - 상태는 반드시 필요한 최소한만 유지한다.
        - 계산 가능한 값은 state로 저장하지 않는다.
        - 불필요한 useState, 중복 상태, 유도 가능한 값 저장 여부 점검.

        2) **선언형 패턴 (Declarative)**
        - 명령형 로직을 선언형으로 바꿀 수 있는지 평가한다.
        - 조건·사이드이펙트·이벤트 흐름이 React스럽게 표현되어 있는지 확인한다.

        3) **역할과 책임 (SRP)**
        - 컴포넌트/함수가 적절한 단일 책임을 가지고 있는지 확인한다.
        - 과도한 로직 포함 또는 과한 분리가 없는지 점검한다.

        4) **반복되는 패턴 / 안티패턴 탐지**
        - PR 전체에서 반복적으로 나타나는 문제 패턴을 식별한다.
        - 개선이 의미 있을 때만 제안하며 오버엔지니어링은 피한다.

        5) **불필요한 렌더링 방지 (Rendering Optimization)**
        - 값이 변하지 않는데 렌더링되는 컴포넌트가 있는지 확인한다.
        - 부모 렌더링이 자식에게 불필요하게 전파되는지 점검한다.
        - useMemo / useCallback은 필요한 경우에만 사용되었는지 확인한다.
        - React.memo는 실제 성능 이점이 있는 곳에만 적용되었는지 평가한다.

        6) **Side Effects / useEffect 책임 분리**
        - useEffect가 단일 책임을 가지는지 확인한다.
        - effect 내부 조건문이 복잡하지 않은지 점검한다.
        - effect 내부에서 state를 여러 번 설정해 불필요한 렌더링을 유발하는지 확인한다.
        - 불필요한 useEffect를 탐지한다.

        7) **컴포넌트 API 일관성 (Props / Naming / Events)**
        - props 이름이 역할을 명확히 드러내는지 확인한다.
        - boolean props는 isX / hasY 패턴을 따른다.
        - 이벤트 핸들러는 onClick / onChange 패턴을 일관적으로 사용한다.
        - 유사한 컴포넌트끼리 props 구조를 일관되게 유지한다.

        ------------------------------------------------------------
        🧾 응답 형식 가이드 (중요도 기반 리뷰)
        ------------------------------------------------------------
        리뷰 결과는 다음 형식을 반드시 따른다:

        1) **가장 중요한 문제 1가지를 최우선으로 강조하여 설명한다.**
           - 이 항목은 리뷰의 맨 앞에서 최상위 강조 포맷으로 제시한다.

        2) **일반적인 개선 포인트는 최대 5개까지만 제시한다.**
           - 중요도 기준으로 선별하여 핵심적인 것만 나열한다.
           - 불필요한 개선, 의미 없는 조언은 제외한다.

        3) 리뷰는 다음 제목으로 시작한다:
           "# ✨ 리액트 코드 구조·상태·역할·선언형 패턴 점검"

        4) 아래 형식에 맞춰 bullet point로 작성한다:
           - 최우선 개선 포인트 1가지 (핵심 문제)
           - 일반 개선 포인트 최대 5가지
           - AS-IS 코드 예시
           - TO-BE 개선 예시
           - 스스로 점검할 수 있는 질문 1개

        ------------------------------------------------------------
        📄 팀 컨벤션(team-conventions.md)도 함께 반영하여 리뷰하세요.
        ------------------------------------------------------------

knowledge_base:
  code_guidelines:
    enabled: true
    filePatterns:
      - "frontend/docs/team-conventions.md"
